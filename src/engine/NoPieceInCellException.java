package engine;

public class NoPieceInCellException extends Exception {
	
	//public NoPieceInCellException(int i, Piece piece){
	//	super("Piece " + i + "does'nt contain any " + (piece.getClass().equals(RedPiece.class) ? "red" : "blue") + " piece."); //TODO ugly, needs visitor
	//}

}
